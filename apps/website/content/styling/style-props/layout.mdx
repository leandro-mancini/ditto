---
title: Layout
description: Propriedades de estilo JSX para controlar a largura, altura e espaçamento dos elementos
---

## Aspect Ratio

Use a propriedade `aspectRatio` para definir a proporção desejada de um elemento.

```jsx
// valor bruto
<Box aspectRatio="1.2" />

// token
<Box aspectRatio="square" />
```

| Prop          | CSS Property   | Token Category |
| ------------- | -------------- | -------------- |
| `aspectRatio` | `aspect-ratio` | `aspectRatios` |

## Break

### Break After

Use a propriedade `breakAfter` para definir como as quebras de página, coluna ou região devem se comportar após um elemento.

```jsx
<Box columns="2">
  <Box>Item 1</Box>
  <Box breakAfter="page">Item 2</Box>
</Box>
```

| Prop         | CSS Property  | Token Category |
| ------------ | ------------- | -------------- |
| `breakAfter` | `break-after` | -              |

### Break Before

Use a propriedade `breakBefore` para definir como as quebras de página, coluna ou região devem se comportar antes de um elemento.

```jsx
<Box columns="2">
  <Box>Item 1</Box>
  <Box breakBefore="page">Item 2</Box>
</Box>
```

| Prop          | CSS Property   | Token Category |
| ------------- | -------------- | -------------- |
| `breakBefore` | `break-before` | -              |

### Break Inside

Use a propriedade `breakInside` para definir como as quebras de página, coluna ou região devem se comportar dentro de um elemento.

```jsx
<Box columns="2">
  <Box>Item 1</Box>
  <Box breakInside="avoid">Item 2</Box>
</Box>
```

| Prop          | CSS Property   | Token Category |
| ------------- | -------------- | -------------- |
| `breakInside` | `break-inside` | -              |

## Box Decoration Break

Use a propriedade `boxDecorationBreak` para definir como as decorações de caixa devem se comportar quando a caixa se quebra em várias linhas, colunas ou páginas.

```jsx /boxDecorationBreak/
<Box bgImage="linear-gradient(red, blue)" boxDecorationBreak="clone">
  Ditto é <br /> ótimo!
</Box>
```

| Prop                 | CSS Property           | Token Category |
| -------------------- | ---------------------- | -------------- |
| `boxDecorationBreak` | `box-decoration-break` | -              |

## Box Sizing

Use a propriedade `boxSizing` para definir o dimensionamento da caixa de um elemento.

```jsx /boxSizing/
<Box boxSizing="border-box" padding="4" width="8" height="8" />
```

| Prop        | CSS Property | Token Category |
| ----------- | ------------ | -------------- |
| `boxSizing` | `box-sizing` | -              |

## Columns

Use a propriedade `columns` para definir o número de colunas para um elemento.

```jsx
<Box columns={2} />
```

| Prop      | CSS Property | Token Category |
| --------- | ------------ | -------------- |
| `columns` | `columns`    | -              |

## Float

Use a propriedade `float` para definir a flutuação de um elemento.

```jsx /float/
<Box>
  <Text>Ditto</Text>
  <Box float="left">Design System</Box>
</Box>
```

| Prop    | CSS Property | Token Category |
| ------- | ------------ | -------------- |
| `float` | `float`      | -              |

## Clear

Use a propriedade `clear` para definir se um elemento deve ser movido abaixo (limpo) de elementos flutuantes que o precedem.

```jsx
<Box>
  <Box float="left">Left</Box>
  <Box float="right">Right</Box>
  <Box clear="none">
    Lorem Ipsum is simply dummy text of the printing and typesetting industry.
    Lorem Ipsum has been the industry's standard dummy text ever since the
    1500s, when an unknown printer took a galley of type and scrambled it to
    make a type specimen book.
  </Box>
</Box>
```

| Prop    | CSS Property | Token Category |
| ------- | ------------ | -------------- |
| `clear` | `clear`      | -              |

## Isolation

Use a propriedade `isolation` para definir se um elemento deve criar explicitamente um novo contexto de empilhamento.

```jsx
<Box isolation="isolate">
  <Box bg="red.500" width="10" height="10" />
</Box>
```

| Prop        | CSS Property | Token Category |
| ----------- | ------------ | -------------- |
| `isolation` | `isolation`  | -              |

## Object Fit

Use a propriedade `objectFit` para definir como uma imagem ou vídeo deve ser redimensionado para se ajustar ao contêiner.

```jsx
<Image src="..." objectFit="cover" width="10" height="10" />
```

| Prop        | CSS Property | Token Category |
| ----------- | ------------ | -------------- |
| `objectFit` | `object-fit` | -              |

## Object Position

Use a propriedade `objectPosition` para definir como um elemento deve ser posicionado dentro de seu contêiner.

```jsx
<Image src="..." objectPosition="center" width="10" height="10" />
```

| Prop             | CSS Property      | Token Category |
| ---------------- | ----------------- | -------------- |
| `objectPosition` | `object-position` | -              |

## Overflow

Use a propriedade `overflow` para controlar como o conteúdo que excede as dimensões de um elemento é tratado. Essa propriedade determina se o conteúdo deve ser recortado, adicionar barras de rolagem ou exibir o conteúdo transbordado.

```jsx
<Box overflow="hidden" maxHeight="120px" />
<Box overflow="auto" maxHeight="120px" />
```

| Prop       | CSS Property | Token Category |
| ---------- | ------------ | -------------- |
| `overflow` | `overflow`   | -              |

## Overscroll Behavior

Use a propriedade `overscrollBehavior` para controlar o que o navegador faz ao atingir o limite de uma área de rolagem.

```jsx
<Box maxHeight="120px" overscrollBehavior="contain" />
```

| Prop                 | CSS Property          | Token Category |
| -------------------- | --------------------- | -------------- |
| `overscrollBehavior` | `overscroll-behavior` | -              |

## Position

Use as utilidades `position` para definir a posição de um elemento.

```jsx
<Box position="absolute" />
<Box pos="absolute" />
```

| Prop       | CSS Property | Token Category |
| ---------- | ------------ | -------------- |
| `position` | `position`   | -              |

## Top / Right / Bottom / Left

Use as utilidades `top`, `right`, `bottom` e `left` para definir a posição de um elemento.

```jsx
<Box position="absolute" top="0" left="0" />

// usando tokens de espaçamento
<Box position="absolute" top="4" />

// usando valores fixos
<Box position="absolute" top="100px" />
```

Use as equivalências lógicas das utilidades `insetStart` e `insetEnd` para definir a posição de um elemento com base no modo de escrita.

```jsx
<Box position="absolute" insetStart="0" />
```

| Prop                                      | CSS Property         | Token Category |
| ----------------------------------------- | -------------------- | -------------- |
| `top`                                     | `top`                | `spacing`      |
| `right`                                   | `right`              | `spacing`      |
| `bottom`                                  | `bottom`             | `spacing`      |
| `left`                                    | `left`               | `spacing`      |
| `start`, `insetStart`, `insetInlineStart` | `inset-inline-start` | `spacing`      |
| `end` , `insetEnd`, `insetInlineEnd`      | `inset-inline-end`   | `spacing`      |
| `insetX`, `insetInline`                   | `inset-inline`       | `spacing`      |
| `insetY`, `insetBlock`                    | `inset-inline`       | `spacing`      |

## Visibility

Use a propriedade `visibility` para controlar a visibilidade de um elemento.

```jsx
<Box visibility="hidden" />
```

| Prop         | CSS Property | Token Category |
| ------------ | ------------ | -------------- |
| `visibility` | `visibility` | -              |

## Z-Index

Use a propriedade `zIndex` para definir o índice Z de um elemento.

```jsx
// usando valores fixos
<Box zIndex="1" />

// usando token
<Box zIndex="overlay" />
```

| Prop     | CSS Property | Token Category |
| -------- | ------------ | -------------- |
| `zIndex` | `z-index`    | `zIndices`     |
